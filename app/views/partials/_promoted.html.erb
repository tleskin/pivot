<div class="container business-info">
  <div class="row">
    <% @businesses.each do |business| %>
      <div class="col-lg-6 col-md-6 col-sm-6">
        <div class="item">
          <%= link_to( image_tag(business.image), region_business_path(region: business.region_id, id: business.id)) %><br>
          <%= business.name %><br>
        </div>
      </div>
    <% end %>
  </div>
</div>
