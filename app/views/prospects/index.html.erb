<div class='container'>
  <div class='row'>
    <div class='col-md-6 col-md-offset-3 text-center'>
      <h3>Prospects</h3>
    </div>
  </div>
  <div class='row'>
    <div class='col-md-6 col-md-offset-3 text-center'>
      <table class='table'>
        <tr>
          <td><strong>Business</strong></td>
          <td><strong>Subtotal</strong><br><em>(Enter new value to change)</em></td>
          <td><strong>Remove Prospect</strong></td>
        </tr>
        <% @prospects_info.each do |prospect| %>
        <tr>
          <div class='prospect'>
            <td><%= link_to "#{prospect.business.name}", region_business_path(region: prospect.business.region_id, id: prospect.business.id) %>
            </td>
            <td class="element" data-business-id= "<%= prospect.business.id  %>" >
              <%= text_field_tag :data, nil, value: prospect.amount, class: 'update-box' %>
            </td>
            <td>
              <%= button_to "remove", prospects_update_path(id: prospect.business.id, change: 'remove'), class: 'btn btn-danger', method: :post %>
            </td>
          </div>
        </tr>
        <% end %>
      </table>
    </div>
  </div>
</div>
<% if logged_in? && prospects? %>
  <%= button_to "Confirm Investment", investments_path, class: 'btn btn-success' %>
<% elsif logged_in? %>
  <p>
    <em>You have no current prospects.</em>
  </p>
<% else %>
  <p>
    <a href="#" data-toggle="modal" data-target="#modal-login" class="btn btn-success">Login to Invest</a>
  </p>
<% end %>
